services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql_agent
    restart: always
    ports:
      - "3308:3306"
    environment:
      MYSQL_DATABASE: ai_agent
      MYSQL_ROOT_PASSWORD: "1234"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
